# <%= projectName %> - Test Automation Framework

AI-powered Playwright test automation framework with BDD support.

## 🚀 Quick Start

### 1. Install Dependencies

```bash
pip install -r requirements.txt
playwright install <%= features.browser || 'chromium' %>
```

### 2. Configure Environment

```bash
cp .env.example .env
# Edit .env with your credentials and configuration
```

### 3. Run Tests

```bash
# Run all tests
behave

# Run with specific tags
behave --tags=@smoke

# Run specific feature
behave features/example.feature
```

## 📁 Project Structure

```
<%= projectName %>/
├── features/              # BDD feature files
│   └── example.feature
├── steps/                 # Step definitions
│   ├── environment.py    # Behave hooks
│   └── common_steps.py   # Reusable steps
├── helpers/              # Helper utilities
│   ├── auth_helper.py           # Authentication
│   ├── healing_locator.py       # Self-healing locators
│   ├── wait_manager.py          # Smart waits
│   ├── data_generator.py        # Test data generation
│   └── screenshot_manager.py    # Auto-screenshots
├── fixtures/             # Test data
├── reports/              # Test reports & screenshots
├── .env                  # Environment configuration
├── behave.ini           # Behave configuration
└── requirements.txt     # Python dependencies
```

## 🎯 Features

### ✅ Self-Healing Locators
Automatically finds elements when locators fail using AI

### ✅ Smart Wait Management
Adapts wait times based on performance history

### ✅ Auto Test Data Generation
Generates realistic test data for every run

### ✅ Reusable Authentication
Login once, reuse across all tests

### ✅ Auto-Screenshot Capture
Every step is automatically captured

<% if (features.powerApps) { %>
### ✅ Power Apps Optimized
Special helpers for Power Apps model-driven applications
<% } %>

## 📝 Writing Tests

### Basic Scenario

```gherkin
Feature: My Feature
  Scenario: My Test
    Given I am logged in
    When I navigate to the "dashboard" page
    Then I should see "Welcome"
```

### Using Test Data

```gherkin
Scenario: Create Contact
  When I fill "First Name" with random "first_name"
  And I fill "Email" with random "email"
```

### Available Steps

See `steps/common_steps.py` for all available step definitions.

## 🔧 Configuration

Edit `.env` file:

```bash
# Application
APP_URL=https://your-app.com

# Credentials
TEST_USER=test@example.com
TEST_PASSWORD=your-password

# AI (for self-healing)
AI_PROVIDER=<%= ai.provider %>
<% if (ai.provider === 'anthropic') { %>ANTHROPIC_API_KEY=your-key<% } %>
<% if (ai.provider === 'openai') { %>OPENAI_API_KEY=your-key<% } %>

# Browser
BROWSER=chromium
HEADLESS=false
```

## 📊 Reports

Test reports are generated in:
- `reports/screenshots/` - Screenshots for each step
- Behave generates console output with pass/fail status

## 🆘 Troubleshooting

### Tests failing with locator errors?
- Enable self-healing: `ENABLE_HEALING=true`
- Check `locator_healing_log.json` for healing attempts

### Tests running slow?
- Check `wait_performance_log.json`
- Run optimization analysis

### Need to debug?
- Set `HEADLESS=false` to see browser
- Add `I pause` step to stop execution
- Screenshots are auto-captured in `reports/screenshots/`

## 📚 Documentation

- [Playwright Python Docs](https://playwright.dev/python/)
- [Behave Docs](https://behave.readthedocs.io/)
- Framework helpers: See docstrings in `helpers/` directory

## 🤝 Support

Generated by [playwright-ai-framework](https://github.com/your-org/playwright-ai-framework)

For issues and questions, refer to the main project documentation.
