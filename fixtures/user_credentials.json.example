# Encrypted User Credentials
# NOTE: In production, use a secure secrets manager!
# This is a template - replace with actual encrypted credentials

{
  "version": "1.0",
  "encrypted": false,
  "users": {
    "admin": {
      "password": "admin_password_here",
      "role": "administrator",
      "permissions": ["read", "write", "delete", "admin"]
    },
    "test_user": {
      "password": "test_password_here",
      "role": "user",
      "permissions": ["read", "write"]
    },
    "viewer": {
      "password": "viewer_password_here",
      "role": "viewer",
      "permissions": ["read"]
    }
  },
  "api_keys": {
    "glm_api_key": "your-glm-api-key",
    "openai_api_key": "your-openai-api-key",
    "anthropic_api_key": "your-anthropic-api-key"
  }
}
